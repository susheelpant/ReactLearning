// notice that we are defining function
// component using arrow function syntax

const AnotherComponent = () => {
  return (
    <div>
      <h2>Another Component</h2>
      <br />
      <p>This is a another React component.</p>
    </div>
  );
};

// must export the component to use it in other files
export default AnotherComponent;
